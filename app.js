console.log(require('./index'))
//"require" is a built-in function in Node JS which gives you access to the exports object of the file that provided the path of.
//It execute the file whichever we specify.

//console.log(module)