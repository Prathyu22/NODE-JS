npm ---> node package manager
We can have external packages through "npm".
We can add, delete packages here.
-------------------------------------------------------------------------------------
Express ---> framework.
npm i express --> installation.
              --> installs globally so that it can be accessed by any node project.

npm init --> installs locally. 
-------------------------------------------------------------------------------
dependencies --> these are the libraries on which the project depends.
-------------------------------------------------------------------------------
***  In Node JS we rely on request and response  ***